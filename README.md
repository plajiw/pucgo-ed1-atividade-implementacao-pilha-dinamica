## Instrução:
Implemente as bibliotecas `node.h` e `pilha.h` (ou `stack.h`) conforme espeficicado no arquivo em anexo.

Também em anexo, segue um primeiro modelo do node.h a ser criado no projeto.

Para `pilha.h`:

```c
typedef struct {
    Node* topo;
    int tamanho;
} Pilha;
```
